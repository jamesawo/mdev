package messages

const (
	RootCmdShortDescription = "Automate development environment setup on macOS"
	RootCmdLongDescription  = `mdev is a command-line tool for setting up and managing
a development environment on macOS.

It installs development tools, configures them, and relocates large
tool caches into a configurable storage path.`
	RootFlagConfirmAll = "Accept all confirmation prompts"
)
